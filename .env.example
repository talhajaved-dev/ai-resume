# Local development
DATABASE_URL=mysql://user:password@localhost:3306/ai_resume_screening
API_PROXY_URL=http://localhost:8080
PORT=8080
# The live prototype uses the bundled deterministic logistic model in apps/api/src/services/model.ts.
# It does not require a .joblib file or external embedding API.
